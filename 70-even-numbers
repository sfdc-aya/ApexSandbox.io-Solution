public List<Integer> evenNumbers(Integer n) {
    List<Integer> nums = new List<Integer>();
    if(n > 0){
        Integer evenNum = 2;

        while(n > 0){
            nums.add(evenNum);
            evenNum+=2;
            n--;
        }
    }
    return nums;
}