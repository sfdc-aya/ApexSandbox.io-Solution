public Map<String, List<Case>> casesByType(List<Case> cases) {
    Map<String, List<Case>> caseMap = new Map<String, List<Case>>();

    for(Case c : cases){
        if(c.Type != null){
            if(caseMap.containsKey(c.Type)){
                caseMap.get(c.Type).add(c);
            }else{
                caseMap.put(c.Type, List<Case> {c});
            }
        }
    }
    return caseMap;
}
